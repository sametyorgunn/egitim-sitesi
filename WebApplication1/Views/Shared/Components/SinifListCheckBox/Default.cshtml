@using EntityLayer.Concrete
@model List<Siniflar>



@foreach (var item in Model)
{
    <div class="checkbox">
        <label>
            <input type="checkbox" name="siniflar"value="@item.Sinif_id"/> @item.Sinif_name
        </label>
    </div>
}



@*@for (int i = 0; i < Model.Count; i++)
{
    <div class="form-check">
        <input type="hidden" name="@Model[i].Sinif_id" />
        <input type="hidden" name="@Model[i].Sinif_name" />
        <input type="checkbox" name="@Model[i].check_box" class="form-check-input" />
        <label class="form-check-label" name="sinif_name" for="@Model[i].check_box">@Model[i].Sinif_name</label>
    </div>
}*@
